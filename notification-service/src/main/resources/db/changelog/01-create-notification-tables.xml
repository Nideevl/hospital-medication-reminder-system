<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

    <changeSet id="01-create-notification-tables" author="system">
        <comment>Create notifications table</comment>

        <!-- Create notifications table -->
        <createTable tableName="notifications">
            <column name="notification_id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="patient_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="notification_type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="recipient" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="subject" type="VARCHAR(255)"/>
            <column name="message" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="sent_time" type="TIMESTAMP"/>
            <column name="retry_count" type="INT" defaultValue="0">
                <constraints nullable="false"/>
            </column>
            <column name="error_message" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Create indexes -->
        <createIndex tableName="notifications" indexName="idx_notifications_patient_id">
            <column name="patient_id"/>
        </createIndex>
        <createIndex tableName="notifications" indexName="idx_notifications_notification_type">
            <column name="notification_type"/>
        </createIndex>
        <createIndex tableName="notifications" indexName="idx_notifications_status">
            <column name="status"/>
        </createIndex>

        <!-- Add foreign key constraints -->
        <addForeignKeyConstraint baseTableName="notifications"
                                 baseColumnNames="patient_id"
                                 constraintName="fk_notifications_patient"
                                 referencedTableName="patients"
                                 referencedColumnNames="patient_id"
                                 onDelete="CASCADE"/>

        <!-- Add CHECK constraints -->
        <addCheckConstraint tableName="notifications"
                            constraintName="chk_notifications_type"
                            checkCondition="notification_type IN (EMAIL, SMS, CALL)"/>

        <addCheckConstraint tableName="notifications"
                            constraintName="chk_notifications_status"
                            checkCondition="status IN (PENDING, SENT, FAILED)"/>

    </changeSet>
</databaseChangeLog>
